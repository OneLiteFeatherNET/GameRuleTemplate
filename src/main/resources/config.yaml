# Below are all supported languages
# You can add own languages by adding a file in the lang folder and adding the language here
# The language file should be named as the language code (e.g. gameruletemplate_de_DE.yaml)
# The language tag code should be added to the list below
languages:
  - us-US
  - de-DE